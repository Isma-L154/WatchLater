# Normalise line endings so generated files (notably worker-configuration.d.ts)
# do not flip between LF and CRLF across machines — a mismatch there makes
# `wrangler types --check`, and therefore the build, fail spuriously.
* text=auto eol=lf
*.png binary
*.svg text eol=lf